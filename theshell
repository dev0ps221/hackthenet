#!/usr/bin/env python3

from core.classes.Shell import Shell


if __name__ == '__main__':
    cmds = {
        'exit':lambda shell : shell.stop(),
        'quit':lambda shell : shell.stop(),
        'aboutshell':lambda shell,name=5 : print(shell)
    }
    shell = Shell(cmds)
    shell.loop()